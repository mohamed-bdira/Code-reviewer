# Required on Vercel (Settings → Environment Variables). Redeploy after changing.
# Railway backend URL only — no trailing slash, no /api suffix.
VITE_API_BASE_URL=https://your-backend.up.railway.app
